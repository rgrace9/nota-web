import React from 'react';
import PropTypes from 'prop-types';

const Homepage = props => {
  return (
    <div>
      <p>HOME APGE</p>
    </div>
  );
};

Homepage.propTypes = {
  
};

export default Homepage;